[project]
name = "panta-tooling"
version = "0.1.0"
description = "Pinned CMake tooling and supplementary C++ analysis for Panta"
requires-python = ">=3.12,<3.15"
dependencies = []

[dependency-groups]
dev = [
    "cmakelang==0.6.13",
    "cppcheck==1.5.1",
]

[tool.uv]
package = false
