[project]
name = "panta-tooling"
version = "0.1.0"
description = "Reproducible CMake formatting and C++ quality tooling for Panta"
requires-python = ">=3.12,<3.15"
dependencies = []

[dependency-groups]
dev = [
    "cmakelang==0.6.13",
]

[tool.uv]
package = false
