#!/bin/sh
# 提交前门禁只做格式检查（秒级、不触发 native 构建）；lint 全套、依赖
# 审计、覆盖率与测试套件由 CI 承担——lint 需要全量 native 构建支撑编译
# 数据库，放进 hook 会让每次提交阻塞数分钟。
# 启用方式（每台机器一次性，git 不随仓库携带 hooks 配置）：
#   git config core.hooksPath .githooks
set -e

echo "[pre-commit] cargo format --check"
cargo format --check

echo "[pre-commit] 通过"
