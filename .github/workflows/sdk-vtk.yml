# VTK 预编译 SDK 生产管线（任务 038；workflow_dispatch 手动触发）。
#
# 受信 CI 语义：按 tools/sdk/ 冻结的构建描述生产制品，自检通过后上传
# 构建产物；`publish` 输入为 true 时把归档与 .sha256 发布到 GitHub
# Release（tag = sdk-vtk-9.7.0），发布说明含源码 pin 与许可证出处
# （tools/sdk/releases/vtk-9.7.0.md）。开发者与普通 CI 经 031 的
# sdk-provision 消费发布归档，不在本地编译 VTK 源码。
#
# runner 工具链：cmake/ninja 为 runner 镜像预装组件（生产管线的受信
# 环境，不是开发者基线）；Qt 依赖沿用 qt-provision 锁定预编译供给并
# 以 actions/cache 做加速层。

name: SDK artifacts (VTK)

on:
  workflow_dispatch:
    inputs:
      publish:
        description: 发布到 GitHub Release（tag sdk-vtk-9.7.0）
        type: boolean
        default: false

concurrency:
  group: sdk-vtk
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  vtk:
    name: vtk 9.7.0 (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            cmake_generator: Ninja
            sdk_triple: macos-arm64
          - os: ubuntu-latest
            cmake_generator: Ninja
            sdk_triple: linux-x86_64
          - os: windows-2022
            cmake_generator: Visual Studio 17 2022
            cmake_generator_platform: x64
            sdk_triple: windows-x86_64
    runs-on: ${{ matrix.os }}
    env:
      CMAKE_GENERATOR: ${{ matrix.cmake_generator }}
      CMAKE_GENERATOR_PLATFORM: ${{ matrix.cmake_generator_platform }}
    steps:
      - uses: actions/checkout@v7

      - name: Install Linux graphics development packages
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            libgl1-mesa-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev

      - name: Cache Qt provision
        uses: actions/cache@v4
        with:
          path: sdk-build/qt
          key: sdk-qt-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('native/cmake/qt-provision.cmake') }}

      - name: Produce VTK SDK
        shell: bash
        run: |
          cmake -S tools/sdk -B sdk-build/superbuild -DQT_PROVISION_DIR="$PWD/sdk-build/qt"
          cmake --build sdk-build/superbuild

      - name: Selfcheck artifact
        shell: bash
        run: |
          cmake -S tools/sdk/selfcheck -B sdk-build/selfcheck \
            -DPSDK_ROOT="$PWD/sdk-build/superbuild/out/vtk/9.7.0/${{ matrix.sdk_triple }}" \
            -DPSDK_PACKAGE=VTK \
            -DPSDK_REQUIRED_TARGETS="VTK::GUISupportQtQuick VTK::RenderingQt" \
            -DCMAKE_PREFIX_PATH="$PWD/sdk-build/qt/staging"

      - name: Package artifact
        shell: bash
        run: |
          cmake -DPKG_ROOT="$PWD/sdk-build/superbuild/out/vtk/9.7.0/${{ matrix.sdk_triple }}" \
            -DPKG_NAME=vtk -DPKG_VERSION=9.7.0 \
            -DPKG_TRIPLE=${{ matrix.sdk_triple }} \
            -DPKG_DIST="$PWD/sdk-build/dist" \
            -P tools/sdk/package.cmake

      - name: Upload build artifact
        uses: actions/upload-artifact@v5
        with:
          name: vtk-9.7.0-${{ matrix.sdk_triple }}
          path: sdk-build/dist/*
          if-no-files-found: error

      - name: Publish release assets
        if: inputs.publish
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # 矩阵各平台并发：Release 不存在则创建（发布说明含许可证/源码
          # 出处），资产以 --clobber 重复上传幂等。
          gh release view sdk-vtk-9.7.0 --repo "$GITHUB_REPOSITORY" \
            || gh release create sdk-vtk-9.7.0 \
                 --repo "$GITHUB_REPOSITORY" \
                 --title "VTK 9.7.0 prebuilt SDKs" \
                 --notes-file tools/sdk/releases/vtk-9.7.0.md
          gh release upload sdk-vtk-9.7.0 sdk-build/dist/* --clobber \
            --repo "$GITHUB_REPOSITORY"
