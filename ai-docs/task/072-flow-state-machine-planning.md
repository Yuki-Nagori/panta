# 072 — Flow DSL 与 Rust 状态机方案评估

- 状态：done
- 阶段：架构与文档准备
- 依赖：[008](008-tasks-errors-logging.md)、[066](066-native-domain-boundaries.md)、[067](067-rust-mesh-domain-migration.md)
- 优先级：P1
- 负责人：Codex
- 创建 / 更新：2026-09-24 / 2026-09-24

## 目标与背景

评估用户提供的「`.pa` Flow DSL + Rust 状态机」方案，结合已存在的 DSL 工具链、任务服务和导入职责，形成可供后续实施的设计与验收边界。当前已有手写任务生命周期，尚无 `kind: flow`、Flow 代码生成或异步导入事务状态机。

## 必读

- [文档规范](../standards/documentation.md)、[仓库文件规范](../standards/repository-hygiene.md)、[验证与评审](../standards/validation-and-review.md)
- [PA 规范](../standards/pa.md)、[分层规则](../standards/layering.md)
- [DSL 工具链](../modules/dsl-engine-and-toolchain.md)、[Rust 领域边界](../architecture/native-domain-boundaries.md)、[应用平台与存储](../architecture/application-and-storage.md)

## 范围与非目标

新增 Flow 设计模块和后续实施任务，同步模块导航、DSL 规范及相关架构入口。评估语法复用、生成目录、权威归属、guard 与转移表、事务失败、取消与迟到结果、测试覆盖。只修改文档，不新增 crate、依赖、parser、生成器或业务状态机，不重写同步 STL 路径。

提交前复审补充检查任务服务与领域事务的终态决定权、关闭期间的提交回执、存储发布结果不确定时的处理，以及生成文件跨输入命名冲突；修正文档后验证并本地提交，不 push。

## 实施步骤

1. 对照用户方案与 `panta-dsl-core`、`panta-import`、`panta-core::project` / `task` 代码。
2. 核验 Cargo 生成产物和 Qt 显示状态的官方依据，记录保留和调整的决策。
3. 写长期设计及后续任务，检查本地链接、索引、语法状态标识与补丁空白。

## 验收标准

- [x] 明确已有能力与规划能力，说明原方案的主要问题及修正。
- [x] 定义 DSL / Rust / Qt 权威、crate 归属、提交失败及异步竞态的边界。
- [x] 后续实施任务有触发条件、范围与可观察验收，已登记索引。
- [x] 文档链接、导航、代码围栏与差异空白验证完成。
- [x] 提交前复审问题已修正，最终文档与提交门禁验证完成。

## 清理与兼容例外

无废弃代码或兼容例外。用户附件是评估输入，不复制为另一份看似已实现的语法规范。

## 验证计划与结果

在仓库根目录执行文档链接 / 围栏检查、任务索引一致性检查与 `git diff --check`；提交前额外执行仓库格式门禁。本次不改可执行代码，不运行应用构建、功能测试或 GPU 窗口验收。

| 日期 | 环境 / 命令或场景 | 实际结果 / 证据 |
|---|---|---|
| 2026-09-24 | 源码与既有设计核对 | 已确认 `.pa` 为冒号语法，parser 共用 `panta-dsl-core`；任务生命周期及工程提交属于 `panta-core`，导入准备属于 `panta-import` |
| 2026-09-24 | macOS arm64 / Python 3.14.0；仓库根目录运行临时标准库脚本，检查本次 Markdown 的相对链接、UTF-8 / LF、文件结尾、围栏语言与闭合、尾随空白、072 / 073 索引状态 | 通过：10 份 Markdown 的本地链接与格式检查，无失效目标；不新增检查框架 |
| 2026-09-24 | 仓库根目录 `git diff HEAD --check` | 退出 0；覆盖暂存和未暂存改动 |
| 2026-09-24 | 手工审阅 DSL 状态标识、职责导航、官方 Cargo / Qt 资料与原方案逐项对照 | 通过；Flow 一律标记为规划，未把草案当作当前 CLI 支持；官方链接与查阅日期保存在设计文档 |
| 2026-09-24 | 提交前复审；核对 `panta-core::task` 的取消 / 终态 / join、`project` 与 `storage` 的写入边界 | 修正 Task 独立终态、关闭 / 修订过滤误丢提交回执、存储结果不确定和输入集合命名问题；将对应条件纳入 073 验收 |
| 2026-09-24 | 临时 Python 脚本复查文档链接 / 格式 / 状态，并静态检查文中提交阶段示例 | 通过；示例 5 状态 / 5 边的引用、唯一性、可达性及终态无出边一致；不是 Flow parser 或业务运行证据 |
| 2026-09-24 | 仓库根目录 `cargo format --check` | 退出 0，`format 检查通过`；满足当前 pre-commit 格式门禁 |

## 决策与工作记录

- 2026-09-24：先登记本任务。保留编译期声明流程图、Rust 手写行为和不提前抽通用 FSM 的方向；修正独立 parser、源码树生成、全流程放入 import、先更新状态再执行易失败 action 等问题。
- 2026-09-24：完成设计与路由同步，073 登记为 planned，等待真实异步消费者接口与已有 DSL 任务收口。无代码、依赖或兼容变更。
- 2026-09-24：用户要求复审后本地 commit。重开文档任务，核对现有 `TaskManager::cancel` / `run_task` 的独立终态与关闭 join 行为，补齐其接入真实导入时必须调整的边界。
- 2026-09-24：复审修正与适用检查完成，未发现阻碍本规划提交的剩余问题；存储结果核对、线程资源和真实业务接口仍为 073 实施前置条件，不能据此宣称运行实现已验证。本次提交包含 072 设计交付和不可分割的 073 后续任务登记，无废弃项或兼容例外。

## 完成摘要

已完成 [Flow 设计](../modules/flow-state-machines.md)、[073 实施任务](073-flow-dsl-and-import-state-machine.md) 及提交前复审，同步相关规范、架构与索引。文档与示例结构检查、格式门禁通过；实际存储恢复、异步业务与跨平台行为待 073 实施验证。本次没有实现或运行 Flow parser / 状态机，不含功能测试或窗口验收证据。
