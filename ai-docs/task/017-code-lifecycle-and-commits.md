# 017 — 代码生命周期与 commit 一致性规范

- 状态：done
- 阶段：仓库维护
- 依赖：无
- 优先级：P1
- 负责人：当前任务执行者
- 创建 / 更新：2026-09-16 / 2026-09-16

## 目标与范围

落实无死代码、及时删除废弃实现、禁止默认兼容分支及每次 commit 的一致性要求。补充规范并复查架构中可能鼓励预留迁移/容错的表述。仅修改文档，不执行 commit、不改写已有暂存内容。

## 必读

- [工作路由](../../AGENTS.md)
- [规范索引](../standards/README.md)
- [任务模板](_template.md)

## 实施步骤

1. 阅读现有注释、评审、仓库文件及协议/存储说明。
2. 明确清理范围、兼容例外标记及退出条件。
3. 编写 commit 格式、功能一致性与 task 同步规则。
4. 更新路由、模板和现有计划任务，验证引用与状态。

## 验收标准

- [x] 明确清理代码、调用点、配置、依赖、测试和文档，不以注释或旧目录保存历史。
- [x] 兼容例外有统一可搜索标记、适用条件、真实任务与清理条件。
- [x] 每次 commit 要求实现/测试/配置/文档/task 一致，支持任务分步提交而不虚报 done。
- [x] 已修正存储/协议相关默认兼容倾向，完成文档检查。

## 清理与兼容例外

仅新增规范，不增加运行时兼容代码。现有仓库无应用源码，本次不声称已完成运行时代码死代码扫描。

## 验证记录

2026-09-16，在仓库根目录执行 Python 检查：55 个 Markdown 文件本地链接/围栏、规范路由、17 个 task 的状态/依赖与无环关系、14 个计划任务的清理验收及 COMPAT 格式一致性全部通过。`git diff --check` 通过。

复查协议未知事件、存储格式迁移和外部共享协议边界，已移除默认兼容实现倾向。当前无应用源码，因此本次仅审查规范和任务，不宣称已完成运行时代码清理。


## 提交前复查

- 2026-09-16：发现前次批量编辑误覆盖 documentation.md、repository-hygiene.md，已恢复两篇原主题内容并保留生命周期要求；补查规范标题唯一性及内容归属。
- 本次将首次文档基线、任务编排、忽略规则与开发约束一并提交，关联 015/016/017；这些入口、规则及相互引用共同形成首次可用的仓库工作约定。基础设施 001–014 仍未实现。
- 无运行时代码或兼容例外；旧入口和重复误写内容已清理。最终验证见提交消息，不预填当前 commit hash。

## 完成摘要

新增代码生命周期与 commit 规范，统一必要兼容的可搜索标记、任务和删除条件；将逐次提交一致性及废弃清理要求写入 AGENTS、模板、14 项计划任务和评审规则。本次无运行时兼容例外、无废弃源码，未执行 Git 提交。
