# 016 — 忽略规则与通用开发规范

- 状态：done
- 阶段：仓库维护
- 依赖：无
- 优先级：P1
- 负责人：当前任务执行者
- 创建 / 更新：2026-09-16 / 2026-09-16

## 目标与范围

按项目工具链配置 .gitignore，补充注释、仓库文件、测试与评审、文档维护规范，并将规则接入任务阅读路径。当前 .gitignore 为空；不实施基础设施源码，不改变已有暂存状态。

## 必读

- [工作路由](../../AGENTS.md)
- [规范索引](../standards/README.md)
- [任务模板](_template.md)

## 实施步骤

1. 核查仓库状态及规划目录，查阅 Git 与语言文档的相关规则。
2. 配置有边界的产物/本机文件忽略规则，保留源码、锁文件和共享配置。
3. 编写通用规范，更新索引、模板与相关基础设施任务。
4. 用正反路径样例检查忽略结果，验证文档链接和任务一致性。

## 验收标准

- [x] 产物与个人配置被忽略；源码、Cargo.lock、共享 presets、样例资产仍可跟踪。
- [x] 注释规范涵盖接口契约、线程/所有权、单位和 TODO，区分官方机制与项目约定。
- [x] 通用规范可从路由/任务模板发现，未宣称工具已落地。
- [x] 忽略规则与文档验证有实际记录，任务与索引状态同步。

## 风险与回退

宽泛规则可能隐藏测试数据和源码，因此使用明确目录和正反样例；不删除已有文件、不修改暂存区，回退只恢复本任务变更。

## 验证记录

2026-09-16，在仓库根目录执行 Python 检查，调用 `git -c core.excludesFile=/dev/null check-ignore --no-index -v -- 路径` 验证 40 个忽略样例与 25 个保留样例，全部符合预期；不创建样例文件、不修改索引。覆盖 Cargo.lock、CMakePresets.json、.cargo/config.toml、环境示例及 STEP/STL/VTK/JSON/log 测试资产。

52 个 Markdown 文件的本地链接、围栏、规范路由、16 个任务的状态/依赖一致性及无环检查通过。`git diff --check` 通过。应用尚无源码，本任务不运行应用测试。


## 完成摘要

已配置项目忽略规则，新增注释、仓库文件、文档、验证与评审四篇通用规范，并更新 AGENTS、规范索引、任务模板及相关基础设施任务的阅读路径。未实现任何基础设施代码，未修改暂存区。
