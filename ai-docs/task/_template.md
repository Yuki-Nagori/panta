# NNN — 任务标题

> 使用方法：复制到 `ai-docs/task/NNN-kebab-case.md`，替换全部占位符后在索引登记。本文是模板，不是待执行任务。文档链接均相对于 `ai-docs/task/` 目录；必读文档按实际任务补充链接。

- 状态：draft
- 阶段：待填写
- 依赖：无，或列出编号与任务文件链接
- 优先级：P0 / P1 / P2，选择其一
- 负责人：待分配
- 创建 / 更新：YYYY-MM-DD / YYYY-MM-DD

## 目标与背景

说明当前问题、触发场景与完成后的可观察行为。简单文档任务可精简不适用章节并说明原因，但必须保留范围、验收和验证记录。区分现有事实、设计假设和待验证项。

## 必读

从 [规范索引](../standards/README.md) 选择相关规则，填写与本任务直接相关的架构及技术规范链接；代码/API 改动引用注释规范，新增文件引用仓库文件规范，文档改动引用文档规范。每次提交阅读 [提交规范](../standards/commits.md)，替换功能或涉及兼容时阅读 [代码生命周期](../standards/code-lifecycle.md)。依赖新 API 时补充官方文档、版本和查阅日期。

## 范围与非目标

列出本次交付和明确不包含的功能。外部仓库内容在此标注 [External / 非本仓库]，不在本仓库顺手实现。

## 前置条件与待决策

列出开始前必须具备的依赖产物、环境条件和待决策项；用条件句描述，未核验时不要写成“已完成”。planned 是等待依赖；blocked 需写具体阻塞与解除条件。不要把未知条件假定为已满足。

## 实施步骤

1. 核实当前实现与依赖，记录必要决策。
2. 填写最小可交付步骤及文件边界。
3. 验证失败路径与主要生命周期，再更新文档。

## 预计改动

列出拟改目录/文件、公共接口、配置、持久化或兼容性影响；说明路径是现存还是待创建。

## 清理与兼容例外

列出将删除的旧实现、调用点、依赖/配置/资源和文档；未涉及替换时写“无废弃项”。默认“无兼容例外”；确有必要时记录 COMPAT 标记位置、适用版本、必要原因、责任模块、验证方式、删除条件与真实清理任务。

## 验收标准

- [ ] 填写可以观察或检查的具体结果，不只写“功能正常”。
- [ ] 关键失败路径和兼容性要求有明确判断条件。
- [ ] 旧实现与失效引用已清理，无未登记兼容分支；例外满足统一标记及退出要求。
- [ ] 每次 commit 的代码、测试、配置、文档和 task 自洽；验证结果已记录，索引状态保持一致。

## 验证计划与结果

区分自动命令、手工检查与暂不适用项；入口不存在时不得伪造命令结果。命令需记录 cwd、工具链和依赖版本。必要时链接小型证据，避免附带敏感数据或大型产物。

| 日期 | 环境 / 命令或场景 | 预期 | 实际结果 / 证据 |
|---|---|---|---|
| — | 待填写 | 待填写 | 未执行 |

## 风险与回退

描述最可能影响本任务的具体风险与恢复步骤。说明如何保留用户已有工作、工程数据或上一份有效产物。

## 决策与工作记录

- YYYY-MM-DD：创建任务。
- 每个 commit 记录本次行为、验证、清理/兼容情况和剩余工作；不要求填写尚未生成的当前 commit hash。
- 记录实际方案、官方依据、范围调整、阻塞原因与解除情况；重大新增范围拆成后续任务。

## 完成摘要

未完成。完成时填写实现结果、验证证据、未覆盖限制及后续 task。检查项有证据、无遗漏的必需工作后再将本任务与索引一起标 done。
